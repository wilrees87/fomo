export interface Item {
  title: string,
  date: string,
  distance?: number,
  status?: string,
  image: string,
  venue?: string,
  place: string,
  lat: number,
  long: number,
  user_handle?: string,
  user_name?: string,
  user_description?: string,
  type: string,
  marker: string
}
